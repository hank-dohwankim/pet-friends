import axios from 'axios';
import {
  PROFILE_LOAD_REQUEST,
  PROFILE_LOAD_SUCCESS,
  PROFILE_LOAD_FAIL,
} from '../constants/profileConstants'

export const loadProfileAction = () => async (dispatch) => {
    try {

    } catch (error) {

    }
};